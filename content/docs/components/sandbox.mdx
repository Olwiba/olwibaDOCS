---
title: Sandbox
description: Interactive multi-file previews for composed examples and advanced UI patterns.
---

`Sandbox` is an additional docs feature for complex examples that need:

- Live preview and code toggle
- Multiple source files in a file explorer
- Desktop, tablet, mobile, and custom preview viewport
- Install command copy and open-in links

Existing `<ComponentPreview />` behavior remains unchanged and should still be used for simple single-component demos.

<Sandbox id="dashboard-overview" />

## Usage

```mdx
<Sandbox id="dashboard-overview" />
```

## Optional Props

```mdx
<Sandbox
  id="dashboard-overview"
  defaultMode="preview"
  defaultViewport="desktop"
  height={640}
/>
```

For full-page shells or sidebar-heavy layouts, use `shellPreview` to give the preview a more natural viewport and internal scrolling:

```mdx
<Sandbox id="dashboard-overview" shellPreview />
```

## API Reference

<APIReference name="Sandbox" props={[
  { name: "id", type: "string", description: "Key into the sandbox registry. Required." },
  { name: "defaultMode", type: '"preview" | "code"', default: '"preview"', description: "Which tab opens first." },
  { name: "defaultViewport", type: '"desktop" | "tablet" | "mobile" | "custom"', default: '"desktop"', description: "Opening preview width. A `defaultViewport` on the registry entry wins over this." },
  { name: "height", type: "number", default: "auto", description: "Fixed preview height in pixels." },
  { name: "shellPreview", type: "boolean", default: "false", description: "Gives the preview a taller viewport and scrolls inside it — for full-page shells and sidebar-heavy layouts." },
]} />

## Notes

- `id` maps to entries in the sandbox registry.
- The built-in registry (`dashboard-overview`) lives in olwibaCN and syncs forward — don't edit `src/components/sandbox-registry.ts` directly, it's `@generated`.
- Consumers register their own sandboxes at app startup with `registerSandboxes` from `@olwiba/docs` (see the README's Sandbox bootstrap section) instead of editing the package registry.
- Use this when the docs example spans multiple files or multiple composed components.
- `shellPreview` is useful for application shells that need a taller, scrollable preview surface.
