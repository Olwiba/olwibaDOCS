---
title: DocsSidebar
description: Sticky navigation sidebar with page tree, section links, active state highlighting, and gradient scroll fades. Hidden on mobile — use DocsMobileNav for mobile navigation.
---

<div className="flex flex-col md:flex-row items-center justify-start gap-2 mb-2">
  <CopyCommandButton command="npm install @olwiba/docs" />
</div>

<ComponentPreview name="docs-sidebar" />

## Import

```tsx
import { DocsSidebar, type SidebarSection } from '@olwiba/docs';
import { SidebarProvider } from '@olwiba/cn';
```

## Usage

```tsx
import { DocsSidebar, type SidebarSection } from '@olwiba/docs';
import { SidebarProvider } from '@olwiba/cn';

const sections: SidebarSection[] = [
  { name: 'Get Started', href: '/docs' },
  { name: 'Components', href: '/docs/components' },
  { name: 'Themes', href: '/docs/themes' },
];

function DocsPage({ pageTree }) {
  return (
    <SidebarProvider>
      <DocsSidebar tree={pageTree} sections={sections} />
      {/* Page content */}
    </SidebarProvider>
  );
}
```

## API Reference

<APIReference
  name="DocsSidebar"
  extends="Sidebar"
  props={[
    { name: "tree", type: "PageTree.Root", default: "-" },
    { name: "sections", type: "SidebarSection[]", default: "[]" },
  ]}
/>
