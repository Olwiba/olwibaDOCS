---
title: CopyButton
description: Clipboard copy button with animated check icon feedback. Used internally by CodeFence, with auto-reset after 2 seconds and ghost variant styling.
---

<div className="flex flex-col md:flex-row items-center justify-start gap-2 mb-2">
  <CopyCommandButton command="npm install @olwiba/docs" />
</div>

<ComponentPreview name="copy-button" />

## Import

```tsx
import { CopyButton } from '@olwiba/docs';
```

## Usage

```tsx
<CopyButton text="npm install @olwiba/docs" />
```

## API Reference

<APIReference
  name="CopyButton"
  props={[
    { name: "text", type: "string", default: "-" },
    { name: "className", type: "string", default: "-" },
  ]}
/>
