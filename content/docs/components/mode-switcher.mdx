---
title: ModeSwitcher
description: Dark/light mode toggle button with Sun/Moon icons, localStorage persistence, and ghost variant styling. Defaults to dark mode.
---

<div className="flex flex-col md:flex-row items-center justify-start gap-2 mb-2">
  <CopyCommandButton command="npm install @olwiba/docs" />
</div>

<ComponentPreview name="mode-switcher" />

## Import

```tsx
import { ModeSwitcher } from '@olwiba/docs';
```

## Usage

```tsx
import { ModeSwitcher } from '@olwiba/docs';

function Header() {
  return (
    <header>
      <ModeSwitcher />
    </header>
  );
}
```

## API Reference

<APIReference
  name="ModeSwitcher"
  props={[]}
/>
