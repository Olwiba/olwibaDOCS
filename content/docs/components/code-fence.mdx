---
title: CodeFence
description: Enhanced code blocks with overflow detection, gradient fade effect, persistent copy button, and theme-aware background colors using the --code CSS variable.
---

<div className="flex flex-col md:flex-row items-center justify-start gap-2 mb-2">
  <CopyCommandButton command="npm install @olwiba/docs" />
</div>

<ComponentPreview name="code-fence" />

## Installation

```sh
npm install @olwiba/docs
```

## Usage

```tsx
import { CodeFence } from '@olwiba/docs';
```

```tsx
<CodeFence code="const hello = 'world';" />
```

`CodeFence` is automatically used as the `pre` element in the `mdxComponents` mapping, so all fenced code blocks in your MDX content use it:

```tsx
import { mdxComponents } from '@olwiba/docs';

<MDX components={{ ...defaultMdxComponents, ...mdxComponents }} />
```

## API Reference

<APIReference
  name="CodeFence"
  props={[
    { name: "children", type: "ReactNode", default: "-" },
    { name: "code", type: "string", default: "-" },
    { name: "className", type: "string", default: "-" },
  ]}
/>
