---
title: DocsMobileNav
description: Sheet-based mobile sidebar navigation with portal trigger pattern. Renders a slide-out sheet for mobile navigation, portaling the trigger button into the site header.
---

<div className="flex flex-col md:flex-row items-center justify-start gap-2 mb-2">
  <CopyCommandButton command="npm install @olwiba/docs" />
</div>

<ComponentPreview name="docs-mobile-nav" />

## Installation

```sh
npm install @olwiba/docs
```

## Usage

```tsx
import { DocsMobileNav } from '@olwiba/docs';
```

Add the trigger container in your header:

```tsx title="SiteHeader.tsx"
<div id="docs-mobile-nav-trigger" className="empty:hidden lg:hidden" />
```

Add `DocsMobileNav` in your docs route — it portals the trigger button into `#docs-mobile-nav-trigger`:

```tsx title="routes/docs/$.tsx"
<DocsMobileNav tree={data.pageTree} sections={sidebarSections} />
```

## API Reference

<APIReference
  name="DocsMobileNav"
  props={[
    { name: "tree", type: "PageTree.Root", default: "-" },
    { name: "sections", type: '{ name: string; href: string }[]', default: "[]" },
  ]}
/>
