export default function DocsProviderDemo() {
  return (
    <div className="text-sm text-fd-muted-foreground">
      <p>DocsProvider wraps your app with RootProvider and a pre-configured SearchDialog.</p>
      <p className="mt-1">It is used at the root layout level and has no visual output on its own.</p>
    </div>
  );
}
